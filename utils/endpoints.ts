import { api_url, next_url } from "./constants";

// External server endpoints
export const postLoginUser = `${api_url}/user/login`;

// Next server endpoints
export const postLoginUserNextApi = `${next_url}/api/`;